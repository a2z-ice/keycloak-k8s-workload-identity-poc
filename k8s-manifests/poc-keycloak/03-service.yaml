apiVersion: v1
kind: Service
metadata:
  name: poc-keycloak
  namespace: poc-keycloak
  labels:
    app: poc-keycloak
spec:
  type: NodePort
  selector:
    app: poc-keycloak
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30888
